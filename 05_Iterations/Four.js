const myObj = {
    py : "Python",
    js : "JavaScript",
    cpp : "c++",
    rb : "ruby"
}

for (const key in myObj) {
    // console.log(`${key} is shortcut of - ${myObj[key]}`);
}

const myArr = ["JS", "PY", "CPP"]
for (const num in myArr) {
    // console.log(`Value is ${myArr[num]}`);   
}

// const map = new Map()

// map.set('IN', "India")
// map.set('USA', "United States of America")
// map.set('Fr', "France")
// map.set('IN', "India")

// console.log(map);

